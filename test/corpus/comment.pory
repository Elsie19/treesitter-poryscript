# foo
const FOO = 5
const BAR = FOO + 2 - 5

script(local) MyScript {
    # hello
    lock
    if (flag(FLAG_TEMP) == TRUE && var(BANG) == FALSE) {
        foo("foo {COLOR BLUE}\n")
    }
    goto
}

mart foo {
    FOO_BAR
    ITEM_NONE
}

text foo {
    "bar\n"
}
